# 사내 실행용 — 여기 적힌 것만 사내 venv 에 설치된다.
# 개발 전용 패키지(anthropic, pytest 등)는 requirements-dev.txt 로 간다 (규격 §1.4).
# 버전은 고정한다. 본 머신과 사내가 같은 것을 쓰게 하는 잠금 파일이다.
#
# 스캐폴드는 표준 라이브러리만 쓴다. 의존성이 생기면 여기에 고정 버전으로 적는다.
