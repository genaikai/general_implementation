# 코드 구현 규격 (Implementation Spec)

본 머신에서 로직을 만들고, 사내 머신으로 이식해 사내 데이터로 실험하고, 그 결과를 다시
본 머신의 로직 개선으로 되돌리는 **반복 워크플로우의 일반 표준**이다.
특정 프로젝트 하나가 아니라, 이 루프를 타는 모든 프로젝트가 이 문서를 따른다.

## 용어

| 기호 | 의미 |
|---|---|
| **BB** | 본 머신에서 개발하는 저장소. 로직의 단일 진실 원본 |
| **AA** | 사내 머신의 작업 폴더. 사내 git으로 관리됨 |
| `<pkg>` | BB가 제공하는 파이썬 패키지 이름. **프로젝트마다 고유해야 한다** |
| 본 머신 | 개인 개발 머신. 실데이터 없음 |
| 사내 머신 | 실데이터가 있는 머신. 외부로 아무것도 내보낼 수 없음 |

---

## §1 목적과 범위

```
[본 머신]                                    [사내 머신]
  (1) 로직 구현  ──── (2) 이식 ────▶  (3) 사내 데이터로 실험
       ▲                                          │
       └────── (4) 인사이트 반영 · 개선 ◀──────────┘
```

**이 문서가 규정하는 것**
- BB 저장소의 레이아웃과, 무엇이 추적되고 무엇이 추적되지 않는가
- BB의 코드가 사내 환경에 닿을 때 지켜야 할 경계
- 사내에서 얻은 것을 본 머신으로 되돌리는 절차

**이 문서가 규정하지 않는 것**
- 도메인 알고리즘의 내용, 모델 선택, 실험 설계
- 코드 스타일(포매터·린터 설정은 프로젝트 재량)

이 규격의 조항 대부분은 §2의 제약에서 기계적으로 따라나온 것이다.
조항이 불편하게 느껴지면 §2로 돌아가 그 제약이 아직 유효한지 확인하라.
제약이 바뀌면 조항도 바꾼다.

---

## §2 불변 제약

| ID | 제약 |
|---|---|
| **C1** | 이식은 BB → AA **단방향**이다. 사내에서 BB로 push할 수 없다 |
| **C2** | 따라서 사내에서 BB 코드를 수정할 수 없다. 수정은 본 머신에서만 일어난다 |
| **C3** | 사내 실데이터는 어떤 형태로도 사내 밖으로 나갈 수 없다 |
| **C4** | 파일·문서 반출이 불가능하다. 결과 파일, 플롯, 로그 파일 모두 못 가져온다 |
| **C5** | 사내에서 회수 가능한 것은 **① 입력 데이터의 포맷**, **② 사람이 얻은 인사이트** 둘뿐이다 |
| **C6** | AA는 사내 git으로 관리된다. AA에 커밋되는 내용에 **가짜/테스트 데이터**와 **BB의 git 설정**이 포함되어서는 안 된다 |
| **C7** | 사내 실행 환경은 Python 3.14 + **이미 존재하는 venv**다. PyPI 설치는 가능하나 그 venv는 공용일 수 있다 |

C4·C5가 이 규격의 성격을 결정한다. 결과를 파일로 주고받는 통상적인 실험 워크플로우가
성립하지 않으므로, **사람의 눈과 손을 거쳐 통과하는 정보만이 루프를 닫는다.**
따라서 (a) 화면 출력이 곧 리포트이고, (b) 데이터 포맷은 코드로 고정되어야 하며,
(c) 인사이트를 코드 변경으로 옮기는 절차가 명시적으로 존재해야 한다.

---

## §3 저장소 레이아웃과 추적 경계

### 3.1 BB 레이아웃

```
BB/
  pyproject.toml          패키지 정의 · 추상 의존성
  requirements.txt        고정 버전 잠금 (커밋됨)
  README.md
  .gitignore              §3.4 — 본문 조항이다
  .gitattributes          export-ignore 지정 (선택)
  src/<pkg>/
    __init__.py
    __main__.py           단일 진입점 (§7)
    contracts/schema.py   입력 데이터 계약 — 단일 출처 (§5)
    synth/generate.py     계약 기반 가상 데이터 생성기 (§6)
    report.py             콘솔 리포트 포매터 (§8)
    ...                   도메인 로직
  configs/example.yaml    설정 템플릿 (커밋됨)
  scripts/sync.sh         사내 이식 스크립트 (§3.3)
  tests/                  파일 픽스처 없음 (§6.3)
  docs/insights/          인사이트 로그 (§11.4)
```

### 3.2 AA 레이아웃

```
AA/
  BB/                  BB 소스 사본. .git 없음. 갱신 시 통째 교체. 손대지 않는다
  configs/local.yaml   사내 전용 설정 — 실데이터 경로 등
  outputs/             실험 산출물 (추적 여부는 사내 정책에 따름)
  notebooks/           사내 탐색 노트북 (§10.2, 추적 여부는 사내 정책에 따름)
  .staging/BB/         이식 중계용 clone (AA에서 무시)
  .staging/.gitignore  내용은 `*` 한 줄
```

**사내 자산은 반드시 `AA/BB` 밖에 둔다.** `AA/BB`는 갱신 때마다 통째로 삭제·재생성되므로,
설정이나 노트북을 그 안에 두면 다음 갱신에 사라진다. 이 배치는 동시에
"BB는 읽기 전용, AA는 사내 전용"이라는 경계를 디렉터리 모양으로 드러낸다.

**AA의 `.gitignore` 최소 요구** — `.staging/`은 반드시 무시한다(C6). `outputs/`와 `notebooks/`의
추적 여부는 사내 정책에 맡기되, 실데이터 원본이나 그 사본은 어떤 경우에도 커밋하지 않는다.

### 3.3 이식 절차

두 사본이 서로 다른 역할을 하나씩 맡는다.

| | `AA/.staging/BB` | `AA/BB` |
|---|---|---|
| 목적 | 외부에서 받아오기 | 실행 · 사내 저장소에 남기기 |
| `.git` | 있음 (그래서 pull 가능) | 없음 (그래서 유출 없고 수정 불가) |
| AA 추적 | ✗ 무시 | ✓ 커밋 |
| 내용 | 전체 히스토리 | 특정 태그의 추적 파일만 |

**최초 1회**

```bash
cd AA
git clone <BB-remote> .staging/BB
printf '*\n' > .staging/.gitignore
bash .staging/BB/scripts/sync.sh <tag>
```

**갱신 (= 사내에서의 "pull")**

```bash
git -C .staging/BB fetch --tags
git -C .staging/BB checkout <tag>
rm -rf BB && mkdir BB
git -C .staging/BB archive <tag> | tar -x -C BB
git -C .staging/BB rev-parse --short HEAD > BB/VERSION
```

`git archive`를 쓰는 이유가 세 겹으로 맞물린다.

1. **`.git`이 결과물에 없다.** 외부 원격 URL도 히스토리도 AA로 넘어가지 않는다 (C6)
2. **추적된 파일만 나온다.** §3.4와 결합하면 데이터·산출물·로컬 설정이 AA로 넘어갈 경로가
   원천적으로 사라진다. `.gitignore`를 신뢰하는 게 아니라, archive가 애초에 담지 않는다
3. **`AA/BB`에 git이 없으므로 사내에서 BB를 고칠 수 없다.** C2가 규칙이 아니라 물리적 상태가 된다

위 갱신 절차는 BB가 `scripts/sync.sh`로 제공한다. 사내에서는 `bash .staging/BB/scripts/sync.sh <tag>`
한 줄로 대체할 수 있으며, 스크립트도 archive 대상이므로 갱신될 때 같이 따라온다.

사내에 보낼 필요가 없는 개발 전용 파일은 `.gitattributes`에 `export-ignore`로 지정해 archive 결과에서
뺀다. 예: `docs/insights/ export-ignore` — 인사이트 로그는 본 머신 전용이다.

`AA/BB`가 AA 저장소에 커밋되는 것은 부수 효과가 아니라 **목적**이다. 결과 파일도 로그도
반출되지 않는 상황에서, 어떤 코드로 그 실험을 돌렸는지가 사내에 남는 유일한 형태다.

**탈출 조항** — AA를 압축 파일이나 파일 복사로 외부에 전달하는 절차가 존재한다면,
중계 clone을 AA 밖(`~/src/BB`)으로 옮긴다. git은 중첩 저장소의 내부를 추적하지 않지만
zip·rsync·백업 도구는 `.git`을 통째로 가져가며, 그때 원격 주소가 그대로 노출된다.

### 3.4 BB의 `.gitignore` (필수)

이것은 부록이 아니라 조항이다. 여기가 뚫리면 C6이 깨진다.

```gitignore
# 데이터 — 확장자 단위로 전면 차단
*.csv
*.tsv
*.parquet
*.xlsx
*.xls
*.json.gz
*.pkl
*.pickle
*.npy
*.npz
*.h5
*.feather
*.db
*.sqlite*

# 데이터·산출물 디렉터리
data/
outputs/
artifacts/
logs/

# 로컬 설정
configs/local.yaml
configs/*.local.yaml
.env

# 노트북
notebooks/local/
.ipynb_checkpoints/

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
```

데이터 확장자는 화이트리스트 없이 전부 막는다. 예외가 필요하면 그때 명시적으로 뚫되,
왜 뚫었는지 커밋 메시지에 남긴다.

### 3.5 자가 점검

BB에서 커밋하기 전, 그리고 AA에서 커밋하기 전:

```bash
git ls-files | grep -E '\.(csv|tsv|parquet|xlsx|xls|pkl|pickle|npy|npz|h5|feather|db|sqlite)$'
git ls-files | grep -E '^\.staging'
```

둘 다 **아무것도 출력하지 않아야 한다.**

---

## §4 4계층 분리 규칙

| 계층 | 어디에 사는가 | 누가 바꾸는가 | 추적 |
|---|---|---|---|
| **코드** | `BB/src/` → `AA/BB/src/` | 본 머신에서만 | BB ✓ / AA ✓ |
| **설정** | `BB/configs/example.yaml` (템플릿), `AA/configs/local.yaml` (실값) | 템플릿은 본 머신, 실값은 사내 | 템플릿 ✓ / 실값 ✗ |
| **데이터** | 사내에만 존재 | — | 항상 ✗ |
| **산출물** | `AA/outputs/` | 실행이 만듦 | 항상 ✗ |

### 4.1 하드코딩 금지 목록

아래는 **코드에 값으로 박아서는 안 된다.** 전부 설정 또는 계약을 통해 들어와야 한다.

- 파일 경로, 디렉터리 경로, 서버 주소, DB 접속 정보
- 컬럼명, 테이블명, 사내 도메인 상수(코드값·등급·부서명 등) → §5 계약으로
- 하이퍼파라미터, 임계값, 학습률, 샘플 수, 날짜 범위
- 데이터 규모 가정(행 수, 메모리 크기), 병렬도, 워커 수

### 4.2 위반 신호

**사내에서 "코드를 한 줄만 고치면 되는데" 하는 상황이 오면, 그건 이미 §4가 깨졌다는 신호다.**
C2 때문에 고칠 수 없고, 고쳐도 다음 갱신에 사라진다. 그 순간 해야 할 일은 코드를 고치는 게
아니라 **"이 값이 설정에 없었다"를 인사이트로 기록해 가지고 나오는 것**이다 (§11.3).

이 비용 구조 때문에, 이 규격은 "바뀔 만한 것은 미리 전부 설정으로 뺀다"는 다소 과한 조항을
허용한다. 평소라면 YAGNI 위반이지만, 여기서는 한 번의 왕복이 사이클 하나를 통째로 버리게
만들기 때문에 값을 한다.

---

## §5 데이터 계약과 검증

C5에 의해 **입력 포맷은 사내에서 회수 가능한 두 가지 중 하나**다.
그 정보가 도착할 지점이 코드 전체에 흩어져 있으면, 사람이 들고 온 포맷 정보를
어디에 반영해야 하는지 매번 헤매게 된다. 그래서 계약은 **파일 하나**에 산다.

### 5.1 계약의 형태

`src/<pkg>/contracts/schema.py` 가 유일한 출처다.

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class Field:
    name: str
    dtype: str                      # "int64" | "float64" | "str" | "datetime64[ns]" | "category"
    nullable: bool
    unit: str | None = None
    allowed: tuple | None = None    # 카테고리 허용값
    rng: tuple | None = None        # (min, max)
    note: str = ""                  # 사내에서 확인된 사실을 적는 자리

INPUT_SCHEMA: tuple[Field, ...] = (
    Field("customer_id", "str",           False, note="영문+숫자 12자리"),
    Field("amount",      "float64",       True,  unit="KRW", rng=(0, 1e12)),
    Field("joined_at",   "datetime64[ns]", False, note="타임존 없음"),
    Field("grade",       "category",      True,  allowed=("A", "B", "C")),
)
```

계약에는 실데이터의 **값**을 적지 않는다. 적는 것은 구조뿐이다 — 이름, 타입, 널 허용,
단위, 허용값 집합, 범위. 이 정도는 포맷 정보에 해당하며 C3에 저촉되지 않는다.
개별 레코드, 실제 분포, 식별 가능한 코드값 목록은 적지 않는다.

### 5.2 검증이 곧 회수 채널

계약은 장식이 아니다. 실행 시작 시 실데이터를 계약에 대조하고,
**불일치를 사람이 그대로 옮겨 적을 수 있는 형태로 출력한다.**

```python
def validate(df) -> list[str]:
    """계약 위반을 사람이 읽는 한 줄씩으로 반환한다."""
```

파일 반출이 불가능하므로(C4), 이 출력이 **입력 포맷을 회수하는 주 채널**이다.
따라서 위반 메시지는 "무엇이 어떻게 달랐는지"를 그 자체로 완결되게 적어야 한다.

```
grade      : unexpected values {'Z', '?'}      ← 계약에 없는 값 2종
amount     : dtype float64 expected, got object
joined_at  : 12,004 nulls but nullable=False
```

"validation failed" 같은 메시지는 이 규격에서 **결함**이다. 사람이 옮겨 적을 것이 없기 때문이다.

### 5.3 계약 갱신 절차

1. 사내에서 위반 메시지를 읽고 옮겨 적는다
2. 본 머신에서 `schema.py`를 고친다. `note`에 확인된 사실을 남긴다
3. 합성 데이터 생성기(§6)와 테스트(§9)가 자동으로 따라온다 — 계약이 단일 출처이므로
4. 새 태그를 낸다 (§10.1)

---

## §6 합성 데이터

### 6.1 데이터는 파일로 존재하지 않는다

C6에 의해, 가짜 데이터가 **파일로 BB에 있으면 AA를 통해 사내 저장소로 흘러들어간다.**
`.gitignore`로 막는 것은 약하다 — `git add -f`, 다른 사람의 조작, 도구의 예외 동작으로
언제든 뚫린다. 그래서 더 강한 규칙을 쓴다.

> **BB는 어떤 데이터 파일도 저장소에 두지 않는다. 가짜 데이터는 생성기 코드로만 존재한다.**

이러면 AA가 BB를 어떤 방식으로 추적하든 **올라갈 파일 자체가 없다.**
`.gitignore`는 실수로 생긴 산출물을 막는 2차 방어로 내려가고, 1차 보증은 구조가 담당한다.

### 6.2 생성기

`src/<pkg>/synth/generate.py` 가 계약을 읽어 런타임에 메모리로 만든다.

```python
def generate(n: int, seed: int = 0, mode: str = "normal"):
    """INPUT_SCHEMA로부터 합성 데이터를 만든다.

    mode="normal"      : 계약을 온전히 만족하는 데이터
    mode="adversarial" : §9.2의 사내 사고 유형을 주입한 데이터
    """
```

- **결정론적이어야 한다.** 같은 `seed`는 같은 데이터를 준다
- 파일로 떨구는 것은 디버깅 목적에 한해 `data/generated/`(무시됨)로만
- 계약이 바뀌면 생성기 출력도 자동으로 바뀐다. 생성기는 계약을 **읽을 뿐** 자체 스키마를 갖지 않는다

### 6.3 테스트 픽스처도 파일이 아니다

`tests/fixtures/*.csv` 같은 파일을 두지 않는다. 테스트는 `generate()`를 호출해 in-memory로
데이터를 만든다. 파일이 없으니 픽스처가 낡을 일이 없고, 계약이 바뀌면 테스트 데이터가
따라 바뀐다.

### 6.4 합성 데이터가 보증하는 것과 못 하는 것

이 구분이 흐려지면 사내 사이클을 낭비한다.

| 보증한다 | 보증하지 못한다 |
|---|---|
| 코드가 끝까지 돈다 (스모크) | 실제 분포에서의 성능·수렴 |
| 계약을 만족하는 입력을 올바로 처리한다 | 계약이 실데이터를 맞게 기술하는지 |
| 타입·널·범위 처리 경로가 존재한다 | 실제 규모에서의 메모리·시간 |
| 인터페이스가 맞물린다 | 인코딩·컬럼명 흔들림 등 현장 잡음 |

오른쪽 열은 **사내에서만 알 수 있다.** 그래서 오른쪽 열의 항목들은 §8 리포트가
반드시 화면에 찍어야 하는 것들이다.

---

## §7 설정과 실행 인터페이스

### 7.1 설정

- `BB/configs/example.yaml` — 모든 키가 등장하는 템플릿. 커밋된다. 값은 안전한 더미
- `AA/configs/local.yaml` — 사내 실값. 추적되지 않으며 `AA/BB` **밖**에 산다

**조기 실패**: 시작 시 설정을 검증하고, 키가 없거나 타입이 다르면 **어떤 계산도 하기 전에**
멈춘다. 30분 돌린 뒤 마지막 단계에서 키 하나 때문에 죽는 것은 사내에서 사이클 하나를 버리는 일이다.

```
config error: 'model.threshold' is required but missing
              (see configs/example.yaml:14)
```

### 7.2 진입점

진입점은 **하나**다. `src/<pkg>/__main__.py`.

```bash
python -m <pkg> --config <path> [--dry-run] [--limit N]
```

| 플래그 | 의미 |
|---|---|
| `--config` | 필수. 설정 파일 경로 |
| `--dry-run` | 실데이터 대신 합성 데이터로 전 구간 스모크. 사내 첫 실행에서 항상 먼저 |
| `--limit N` | 앞 N행만 처리. 계약 검증과 파이프라인 확인용 |

`--dry-run`과 `--limit`은 사치가 아니다. 사내에서 실패를 발견하는 시점을 앞으로 당기는
유일한 수단이고, 왕복 한 번의 비용이 크기 때문에 값을 한다.

### 7.3 사내 실행 절차

```bash
# ① 갱신
git -C .staging/BB fetch --tags && git -C .staging/BB checkout <tag>
bash .staging/BB/scripts/sync.sh <tag>   # §3.3 — archive로 AA/BB 통째 교체 + VERSION 갱신

# ② 환경 (기존 venv 사용)
source <기존 venv>/bin/activate
pip install --dry-run -r BB/requirements.txt   # 먼저 확인
pip check                                       # 기존 의존성 정합성
pip install -r BB/requirements.txt              # 충돌이 없을 때만

# ③ 실행
PYTHONPATH=BB/src python -m <pkg> --config configs/local.yaml --dry-run
PYTHONPATH=BB/src python -m <pkg> --config configs/local.yaml
```

### 7.4 의존성 조항

- **BB 패키지를 venv에 설치하지 않는다.** `PYTHONPATH`로만 붙인다.
  공용 venv를 오염시키지 않고, 프로젝트를 여럿 돌려도 같은 이름끼리 덮어쓰지 않으며,
  `AA/BB` 통째 교체가 완전한 무연산이 된다
- `pyproject.toml`은 유지한다. 본 머신 개발(`pip install -e .`)과 의존성 명세의 단일 출처
- `requirements.txt`는 **고정 버전**으로 커밋한다. 본 머신과 사내가 같은 버전을 쓰게 하는 잠금 파일
- **버전 충돌을 사내에서 해결하지 않는다.** `--upgrade`, `--force-reinstall`은 금지다.
  그 venv를 쓰는 다른 작업이 조용히 깨지고, 되돌릴 수 없다.
  충돌 메시지를 인사이트로 가지고 나와 본 머신에서 버전 범위를 조정하고 새 태그를 낸다
- 본 머신도 **Python 3.14**를 쓴다. 3.14용 wheel이 없는 패키지를 본 머신에서 미리 걸러내기 위함이다

---

## §8 콘솔 리포트 규격

C4에 의해 **콘솔이 리포트다.** 로그 파일을 잘 남기는 설계는 여기서 값을 하지 않는다.
사람이 화면을 보고 판단을 끝내고, 필요한 것을 옮겨 적을 수 있어야 한다.

### 8.1 고정 포맷

실행이 어떻게 끝나든(성공·실패·중단) 마지막에 이 블록을 찍는다.

```
================ RUN SUMMARY ================
version   : v0.4 (a1b2c3d)
config    : configs/local.yaml (sha256:9f2c1e…)
input     : 1,204,331 rows x 27 cols
contract  : 24 ok / 3 MISMATCH
  - grade      : unexpected values {'Z', '?'}
  - amount     : dtype float64 expected, got object
  - joined_at  : 12,004 nulls but nullable=False
metrics   :
  auc              0.8123
  precision@100    0.4410
  runtime_sec      412
  peak_mem_gb      6.2
status    : OK
=============================================
```

### 8.2 반드시 포함할 것

| 항목 | 이유 |
|---|---|
| 버전 (`BB/VERSION`의 태그+SHA) | 인사이트가 어느 코드에서 나온 것인지 따라붙어야 한다 |
| 설정 요약 또는 해시 | 같은 코드·다른 설정의 실행을 구분한다 |
| 입력 규모 (행×열) | §6.4의 "보증 못 하는" 축 |
| 계약 검증 결과 전문 | 포맷 회수의 주 채널 (§5.2) |
| 핵심 지표 | 판단의 근거 |
| 피크 메모리·런타임 | 실규모에서만 드러나는 축 |
| 실패 시 원인 한 줄 | 스택트레이스가 아니라 사람이 옮길 수 있는 요약 |

### 8.3 작성 규칙

- **한 줄에 한 항목**, 라벨은 짧게, 폭은 80자 이내. 손으로 옮겨 적는 것이 전제다
- 지표 이름은 사이클 사이에 **바뀌지 않는다.** 이름이 흔들리면 지난 사이클과 비교할 수 없다
- 실데이터의 개별 값·식별자를 찍지 않는다 (C3). 집계·구조·개수만 찍는다
- 실패해도 이 블록은 찍는다. 실패한 실행이야말로 가져올 정보가 많다

---

## §9 테스트 규격

본 머신에는 실데이터가 없다. 테스트의 목적은 **성능 검증이 아니라, 사내에서 낭비될
사이클을 미리 없애는 것**이다.

### 9.1 필수 테스트

| 종류 | 내용 |
|---|---|
| 계약 왕복 | `generate()` 출력이 `validate()`를 통과한다 |
| 전 구간 스모크 | 합성 데이터로 진입점부터 리포트 출력까지 끝까지 돈다 |
| 설정 검증 | `example.yaml`의 모든 키가 실제로 쓰이고, 누락 시 조기 실패한다 |
| 리포트 포맷 | 성공·실패 양쪽에서 §8 블록이 찍히고 필수 항목이 모두 들어있다 |

### 9.2 사내에서만 터지는 것 (적대적 모드)

`generate(mode="adversarial")`가 아래를 주입한다. 각각에 대응 테스트를 둔다.

- 숫자 컬럼이 `object`로 읽힘 (공백, 콤마, `-`, 빈 문자열 혼입)
- 계약에 없는 카테고리 값, 대소문자·앞뒤 공백이 다른 값
- 컬럼명에 공백·대소문자 차이·BOM
- 인코딩 차이 (cp949 / utf-8-sig)
- 날짜 포맷 혼재, 타임존 유무
- `nullable=False` 컬럼의 결측
- 키 중복, 예상 밖의 행 수 (0행, 매우 큰 규모)

이 목록은 **사이클을 돌면서 늘어난다.** 사내에서 새로 터진 유형은 인사이트로 가지고 나와
여기에 항목으로 추가하는 것이 §11.4의 일부다.

---

## §10 버전 추적과 노트북

### 10.1 버전

- 사내는 **브랜치가 아니라 태그**를 받는다. `v<major>.<minor>` 로 단조 증가
- 태그 없이 사내에서 실행하지 않는다. "지금 main"으로 실험하면 어느 코드였는지 재현 불가
- `BB/VERSION`(태그+짧은 SHA)이 §8 리포트 첫 줄로 찍힌다
- `VERSION`에 **원격 주소는 적지 않는다** (C6)
- 사내에서 실험한 태그는 AA 커밋에 `AA/BB`와 함께 남는다. 이것이 사내에 남는 유일한 재현 수단이다

### 10.2 노트북 (탐색 허용)

사내에서의 자유로운 탐색은 인사이트의 주 생산 수단이므로 명시적으로 허용한다.
단, 조건이 붙는다.

- 위치: `AA/notebooks/` (AA에서 무시). BB에는 실행 노트북을 두지 않는다
- **로직은 노트북에 살지 않는다.** 노트북은 `PYTHONPATH=BB/src`로 `<pkg>`를 import해서 호출만 한다.
  노트북에서 만든 로직은 그 사이클이 끝나면 사라진다 — 반출할 수 없기 때문이다
- **세션 종료 시 인사이트 요약이 의무다.** 노트북 자체는 못 가져오므로, 화면에서 판단이 끝나고
  요약이 남지 않으면 그 탐색은 통째로 증발한다

---

## §11 사이클 체크리스트

### 11.1 (1) 본 머신에서 로직 구현

- [ ] 계약(`schema.py`)을 먼저 정하거나 갱신했다
- [ ] 바뀔 만한 값이 전부 `configs/example.yaml`에 있다 (§4.1)
- [ ] `--dry-run`이 끝까지 돈다
- [ ] 적대적 모드 테스트가 통과한다 (§9.2)
- [ ] §8 리포트가 성공·실패 양쪽에서 찍힌다
- [ ] `git ls-files`에 데이터 파일이 없다 (§3.5)
- [ ] `requirements.txt`가 3.14에서 실제로 설치된다
- [ ] 태그를 냈다

### 11.2 (2) 사내 이식

- [ ] `.staging`에서 태그를 checkout했다
- [ ] `git archive`로 `AA/BB`를 통째 교체했다 (`.git` 없음)
- [ ] `BB/VERSION`이 갱신됐다
- [ ] `pip install --dry-run` + `pip check`로 충돌을 먼저 확인했다
- [ ] AA 커밋 전 `git ls-files` 점검을 통과했다 (§3.5)

### 11.3 (3) 사내 실험

- [ ] `--dry-run` → `--limit N` → 전체 순으로 올라갔다
- [ ] 계약 검증 결과를 **전부** 옮겨 적었다 (일부만 적으면 다음 사이클에 또 터진다)
- [ ] 리포트의 지표·규모·메모리·런타임을 옮겨 적었다
- [ ] 실패했다면 원인 한 줄을 옮겨 적었다
- [ ] "코드를 고치고 싶었던 순간"을 전부 적었다 — 그것이 §4 위반 목록이다
- [ ] 사내 밖으로 나가는 것은 **텍스트로 옮겨 적은 것뿐**임을 확인했다 (C3·C4)

### 11.4 (4) 본 머신으로 반영

루프에서 사람 머릿속을 거치는 유일한 고리이며, 가장 잘 새는 지점이다.

- [ ] 실험 직후 `docs/insights/YYYY-MM-DD-<tag>.md`에 기록했다 — **기억이 사라지기 전에**
- [ ] 기록에 포함: 실행한 태그 / 설정 요약 / 계약 위반 전문 / 지표 / 실패 원인 /
      "고치고 싶었던 순간" 목록 / 다음에 시도할 것
- [ ] 계약 위반 → `schema.py` 갱신 (§5.3)
- [ ] 새로 터진 사고 유형 → `generate(mode="adversarial")`에 항목 추가 (§9.2)
- [ ] "고치고 싶었던 순간" → 해당 값을 설정으로 승격 (§4.1)
- [ ] 의존성 충돌 → `requirements.txt` 범위 조정
- [ ] 새 태그를 내고 (1)로 돌아간다

---

## §12 금지 사항 요약

| | 금지 |
|---|---|
| 1 | 사내에서 `AA/BB` 코드를 수정하는 것 |
| 2 | BB에 데이터 파일을 커밋하는 것 (테스트 픽스처 포함) |
| 3 | `AA/BB`에 `.git`을 두는 것 |
| 4 | `AA/BB` 안에 사내 설정·노트북·산출물을 두는 것 |
| 5 | 태그 없이 사내에서 실행하는 것 |
| 6 | 사내에서 `pip install --upgrade` / `--force-reinstall` |
| 7 | BB 패키지를 사내 venv에 설치하는 것 |
| 8 | 리포트에 실데이터의 개별 값·식별자를 찍는 것 |
| 9 | 경로·컬럼명·임계값을 코드에 하드코딩하는 것 |
| 10 | 사내 탐색 세션을 인사이트 기록 없이 끝내는 것 |
